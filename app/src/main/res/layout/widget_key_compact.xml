<?xml version="1.0" encoding="utf-8"?>
<!--
    Action Key, smallest useful size (roughly 1x1). Icon and one word: at this size a second line
    of text is unreadable, so it is simply not drawn rather than squeezed in.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_surface"
    android:padding="10dp"
    android:theme="@style/Theme.TapRelayWidget">

    <ImageView
        android:id="@+id/key_icon"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:src="@drawable/ic_w_scene"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/key_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text"
        android:text="@string/widget_default_name" />
</LinearLayout>
