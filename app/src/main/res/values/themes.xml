<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TapRelay" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.TapRelay.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--
        Widgets are inflated inside the launcher's process, so they get no theme of ours unless a
        layout asks for one. This carries nothing but the text appearance defaults, so a launcher
        with an unusual theme cannot leave TapRelay's widget unreadable.
    -->
    <style name="Theme.TapRelayWidget" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:textColorPrimary">@color/widget_text</item>
        <item name="android:textColorSecondary">@color/widget_text_dim</item>
    </style>
</resources>
